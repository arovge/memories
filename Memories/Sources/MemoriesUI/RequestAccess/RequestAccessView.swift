import SwiftUI

struct RequestAccessView: View {
    var body: some View {
        Text("This app does not have access to your photos to find memories")
        Text("Please go to settings and allow access")
        // TODO: Link to settings to allow access?
    }
}
